import { Entry } from 'helpers/getJSONData'
import { ComponentChildren } from 'preact'

export default interface ChildrenProp {
  children: ComponentChildren
}
// export default interface ChildrenEntry {
//   children: Entry
// }
